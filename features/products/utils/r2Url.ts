// features/products/utils/r2Url.ts
//
// Re-exported from the shared lib so there's exactly one implementation
// used across the whole app (admin product table, cart, product detail
// pages, etc.) instead of three separate copies.
export { getImageUrl } from "@/lib/image";